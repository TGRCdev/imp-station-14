<PanelContainer
    xmlns="https://spacestation14.io"
    xmlns:panels="clr-namespace:Content.Client._Impstation.Teg.UI.Panels"
    StyleClasses="BackgroundPanel"
>
    <BoxContainer Orientation="Vertical">
        <Label Name="CircTitle" />
        <GridContainer Columns="2">
            <Label Text="P Δ" />
            <PanelContainer StyleClasses="LCD" HorizontalAlignment="Right" MinWidth="220" Margin="6 0 0 0">
                <Label Name="PressureDelta" StyleClasses="LCD" HorizontalAlignment="Right" Margin="3"/>
            </PanelContainer>

            <Label Text="MOLE Δ" />
            <PanelContainer StyleClasses="LCD" HorizontalAlignment="Right" MinWidth="220" Margin="6 0 0 0">
                <Label Name="MolesTransferred" StyleClasses="LCD" HorizontalAlignment="Right" Margin="3"/>
            </PanelContainer>

            <Label Text="TEMP" />
            <PanelContainer StyleClasses="LCD" HorizontalAlignment="Right" MinWidth="220" Margin="6 0 0 0">
                <Label Name="Temperature" StyleClasses="LCD" HorizontalAlignment="Right" Margin="3"/>
            </PanelContainer>
        </GridContainer>
    </BoxContainer>
</PanelContainer>
